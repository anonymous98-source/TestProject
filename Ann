console.log(
  "selectedYear =", selectedYear, typeof selectedYear,
  "minYear =", minYear, typeof minYear,
  "maxYear =", maxYear, typeof maxYear
); 

console.log(
  "minDate =", minYear ? dayjs(`${minYear}-01-01`) : null,
  "maxDate =", maxYear ? dayjs(`${maxYear}-12-31`) : null
);
