JSplitPane tablesSplit = new JSplitPane(
        JSplitPane.VERTICAL_SPLIT,
        allocationScroll,
        groupScroll
);

// 🔥 Give more height to Group Summary
tablesSplit.setDividerLocation(220);   // was 260
tablesSplit.setResizeWeight(0.45);     // more space to bottom table
tablesSplit.setOneTouchExpandable(true);