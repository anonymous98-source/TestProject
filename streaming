Now that you've shared the actual SearchAutocompleteComponent, we can implement this exactly in this component. You do not need to modify the AdditionPanelComponent HTML/TS for this requirement.

The good news is that your Location API response is already mapping active correctly:

{
  id: loc.id,
  name: loc.name,
  active: loc.active,
}

So we only need to make inactive Location options disabled.

There is one important detail in your code: you have an opti-checkbox inside the mat-option, and its click handler directly calls toggleSelection(option). Therefore, just adding [disabled] to mat-option is not sufficient. We should disable it visually and also protect toggleSelection().


---

1. Change your HTML

Replace this existing section:

<ng-container *ngIf="filteredOptions | async as options">
  <mat-option
    *ngFor="let option of options"
    [value]="option"
    class="custom-option"
  >
    <opti-checkbox
      [checked]="isSelected(option)"
      (click)="$event.stopPropagation()"
      (checkboxChange)="toggleSelection(option)"
      [noFormGroup]="true"
      [title]="
        searchPlaceholder() === AddPanelType.BillingGroup
          ? option.name + ' (' + option.id + ')'
          : option.name
      "
    >
    </opti-checkbox>
  </mat-option>
</ng-container>

with this:

<ng-container *ngIf="filteredOptions | async as options">
  <mat-option
    *ngFor="let option of options"
    [value]="option"
    [disabled]="isOptionDisabled(option)"
    class="custom-option"
  >
    <opti-checkbox
      [checked]="isSelected(option)"
      [disabled]="isOptionDisabled(option)"
      (click)="$event.stopPropagation()"
      (checkboxChange)="toggleSelection(option)"
      [noFormGroup]="true"
      [title]="
        searchPlaceholder() === AddPanelType.BillingGroup
          ? option.name + ' (' + option.id + ')'
          : option.name
      "
    >
    </opti-checkbox>
  </mat-option>
</ng-container>

The two important additions are:

[disabled]="isOptionDisabled(option)"

on mat-option, and:

[disabled]="isOptionDisabled(option)"

on opti-checkbox.


---

2. Add this method to your TS

Add this inside SearchAutocompleteComponent:

isOptionDisabled(option: any): boolean {
  return (
    this.searchPlaceholder() === AddPanelType.Location &&
    option?.active === false
  );
}

So the logic is specifically:

Panel = Location
        +
active = false
        ↓
disabled = true

For Customer and Billing Group, this method returns false, so their existing behavior doesn't change.


---

3. Modify onOptionSelected()

Your current method is:

onOptionSelected(event: MatAutocompleteSelectedEvent): void {
  const selectedEntry = event.option.value;
  const current = this.control.value || [];
  if (!current.some((item) => item.id === selectedEntry.id)) {
    this.control.setValue([...current, selectedEntry]);
  }
  this.searchInputControl.setValue('');
}

Change it to:

onOptionSelected(event: MatAutocompleteSelectedEvent): void {
  const selectedEntry = event.option.value;

  // Safety check: inactive locations must never be selected
  if (this.isOptionDisabled(selectedEntry)) {
    return;
  }

  const current = this.control.value || [];

  if (!current.some((item) => item.id === selectedEntry.id)) {
    this.control.setValue([...current, selectedEntry]);
  }

  this.searchInputControl.setValue('');
}

This is a second layer of protection.


---

4. Modify toggleSelection()

This is actually very important in your implementation because your checkbox has:

(checkboxChange)="toggleSelection(option)"

So change your current method:

toggleSelection(option: any) {
  const current = this.control.value || [];
  if (this.isSelected(option)) {
    this.control.setValue(current.filter((item) => item.id !== option.id));
  } else {
    this.control.setValue([...current, option]);
  }
}

to:

toggleSelection(option: any): void {
  // Do not allow inactive locations to be selected
  if (this.isOptionDisabled(option)) {
    return;
  }

  const current = this.control.value || [];

  if (this.isSelected(option)) {
    this.control.setValue(
      current.filter((item) => item.id !== option.id)
    );
  } else {
    this.control.setValue([...current, option]);
  }
}

Now even if somebody somehow triggers the checkbox event, an inactive Location cannot enter selectedItemsControl.


---

5. Your Location API mapping is already correct

Keep this exactly as you currently have it:

case AddPanelType.Location:
  return this.locationService.search(this.searchValue).pipe(
    map((res) =>
      (res?.customerLocations || []).map((loc: any) => ({
        id: loc.id,
        name: loc.name,
        active: loc.active,
      }))
    ),
    switchMap((transformed) => this.handleSearch(of(transformed)))
  );

You do not need to filter this response.

That's important because you want inactive locations visible but disabled.


---

6. For testing without the API

You can temporarily mock the Location API response directly in your TS.

Add this property inside your class:

private readonly mockLocationResponse = {
  customerLocations: [
    {
      id: 'LOC001',
      name: 'Pune',
      active: true,
    },
    {
      id: 'LOC002',
      name: 'Mumbai',
      active: false,
    },
    {
      id: 'LOC003',
      name: 'Bangalore',
      active: true,
    },
    {
      id: 'LOC004',
      name: 'Delhi',
      active: false,
    },
    {
      id: 'LOC005',
      name: 'Hyderabad',
      active: true,
    },
  ],
};

Put it somewhere around:

searchValue = '';
inputMaxCharacters = 100;

For example:

searchValue = '';
inputMaxCharacters = 100;

private readonly mockLocationResponse = {
  customerLocations: [
    {
      id: 'LOC001',
      name: 'Pune',
      active: true,
    },
    {
      id: 'LOC002',
      name: 'Mumbai',
      active: false,
    },
    {
      id: 'LOC003',
      name: 'Bangalore',
      active: true,
    },
    {
      id: 'LOC004',
      name: 'Delhi',
      active: false,
    },
    {
      id: 'LOC005',
      name: 'Hyderabad',
      active: true,
    },
  ],
};


---

7. Temporarily use the mock response

Now temporarily replace your Location case with:

case AddPanelType.Location:
  return of(this.mockLocationResponse).pipe(
    map((res) =>
      (res?.customerLocations || []).map((loc: any) => ({
        id: loc.id,
        name: loc.name,
        active: loc.active,
      }))
    ),
    switchMap((transformed) => this.handleSearch(of(transformed)))
  );

You already import:

import { merge, Observable, of } from 'rxjs';

so no new import is required.


---

8. What you should see

When you select Location and click the search field, you should get:

Pune          ☑ / selectable
Mumbai        ☐ / disabled
Bangalore     ☑ / selectable
Delhi         ☐ / disabled
Hyderabad     ☑ / selectable

The inactive ones:

Mumbai
Delhi

should remain visible but be disabled.

You should not be able to select them either by clicking the row or by clicking the checkbox.


---

9. Full relevant TS after changes

So your important methods should look like this:

isOptionDisabled(option: any): boolean {
  return (
    this.searchPlaceholder() === AddPanelType.Location &&
    option?.active === false
  );
}

onOptionSelected(event: MatAutocompleteSelectedEvent): void {
  const selectedEntry = event.option.value;

  if (this.isOptionDisabled(selectedEntry)) {
    return;
  }

  const current = this.control.value || [];

  if (!current.some((item) => item.id === selectedEntry.id)) {
    this.control.setValue([...current, selectedEntry]);
  }

  this.searchInputControl.setValue('');
}

isSelected(option: any): boolean {
  const current = this.control.value || [];
  return current.some((item) => item.id === option.id);
}

toggleSelection(option: any): void {
  if (this.isOptionDisabled(option)) {
    return;
  }

  const current = this.control.value || [];

  if (this.isSelected(option)) {
    this.control.setValue(
      current.filter((item) => item.id !== option.id)
    );
  } else {
    this.control.setValue([...current, option]);
  }
}

And the HTML:

<ng-container *ngIf="filteredOptions | async as options">
  <mat-option
    *ngFor="let option of options"
    [value]="option"
    [disabled]="isOptionDisabled(option)"
    class="custom-option"
  >
    <opti-checkbox
      [checked]="isSelected(option)"
      [disabled]="isOptionDisabled(option)"
      (click)="$event.stopPropagation()"
      (checkboxChange)="toggleSelection(option)"
      [noFormGroup]="true"
      [title]="
        searchPlaceholder() === AddPanelType.BillingGroup
          ? option.name + ' (' + option.id + ')'
          : option.name
      "
    >
    </opti-checkbox>
  </mat-option>
</ng-container>

One thing to verify

Your OptiCheckboxComponent needs to support a disabled input. If IntelliSense/compiler gives an error on:

[disabled]="isOptionDisabled(option)"

for opti-checkbox, don't remove the protection from toggleSelection(). The selection will still be prevented, but we'll need to check the OptiCheckboxComponent API to get the correct property for its disabled/greyed-out appearance.

Also, once you're done testing, remove mockLocationResponse and restore the real:

this.locationService.search(this.searchValue)

The actual API response you need is essentially:

{
  "customerLocations": [
    {
      "id": "LOC001",
      "name": "Pune",
      "active": true
    },
    {
      "id": "LOC002",
      "name": "Mumbai",
      "active": false
    }
  ]
}

Your existing mapping already converts this into exactly the structure required.