Yeah… this isn’t “final boss UI” yet 😄 — this is still structured Swing, not designed UI.

What you’re missing is not code… it’s design system + visual hierarchy.

Right now your app:

❌ uses default colors

❌ no accent color (purple)

❌ no selected state

❌ no typography hierarchy

❌ no visual depth


👉 Let’s fix that properly.


---

🚀 Phase 7: Real UI Design System (This changes EVERYTHING)

We’re going to introduce:

🎯 What you’ll get after this step

Purple accent theme (like your image)

Selected sidebar highlight

Modern typography

Header (Snippets / Editor ●)

Clean spacing + sections



---

🎨 Step 1: Define Theme (VERY IMPORTANT)

Create this class:

📄 Theme.java

package com.devworkbench.util;

import java.awt.*;

public class Theme {

    public static final Color BACKGROUND = new Color(24, 26, 31);
    public static final Color SIDEBAR = new Color(20, 22, 26);
    public static final Color CARD = new Color(32, 34, 40);

    public static final Color ACCENT = new Color(130, 90, 255); // 🔥 PURPLE
    public static final Color HOVER = new Color(45, 48, 54);

    public static final Color TEXT_PRIMARY = new Color(220, 220, 220);
    public static final Color TEXT_SECONDARY = new Color(150, 150, 150);

}


---

🧭 Step 2: Sidebar with Selected Highlight (THIS IS THE MAGIC)

Replace your createMenuItem with this:

private JButton createMenuItem(String text) {
    JButton btn = new JButton(text);
    btn.setHorizontalAlignment(SwingConstants.LEFT);
    btn.setFocusPainted(false);
    btn.setBorder(BorderFactory.createEmptyBorder(12, 20, 12, 10));
    btn.setFont(new Font("Segoe UI", Font.PLAIN, 14));
    btn.setForeground(Theme.TEXT_SECONDARY);
    btn.setBackground(Theme.SIDEBAR);
    btn.setCursor(new Cursor(Cursor.HAND_CURSOR));

    btn.addActionListener(e -> {
        switchPanel(text);
        highlightSelected(btn);
    });

    btn.addMouseListener(new java.awt.event.MouseAdapter() {
        public void mouseEntered(java.awt.event.MouseEvent evt) {
            if (!btn.getBackground().equals(Theme.ACCENT)) {
                btn.setBackground(Theme.HOVER);
            }
        }

        public void mouseExited(java.awt.event.MouseEvent evt) {
            if (!btn.getBackground().equals(Theme.ACCENT)) {
                btn.setBackground(Theme.SIDEBAR);
            }
        }
    });

    return btn;
}


---

🔥 Add THIS method (IMPORTANT)

private void highlightSelected(JButton selectedBtn) {
    for (Component comp : selectedBtn.getParent().getComponents()) {
        if (comp instanceof JButton) {
            comp.setBackground(Theme.SIDEBAR);
            comp.setForeground(Theme.TEXT_SECONDARY);
        }
    }

    selectedBtn.setBackground(Theme.ACCENT);
    selectedBtn.setForeground(Color.WHITE);
}


---

👉 Now you’ll get:

Purple selected menu

Clean hover

Real app feel



---

🧠 Step 3: Add Header (LIKE IMAGE)

Inside SnippetPanel, add this ABOVE toolbar:

private JPanel createHeader() {
    JPanel panel = new JPanel(new BorderLayout());
    panel.setBackground(Theme.BACKGROUND);

    JLabel title = new JLabel("Snippets / Editor ●");
    title.setFont(new Font("Segoe UI", Font.BOLD, 18));
    title.setForeground(Color.WHITE);

    panel.add(title, BorderLayout.WEST);

    return panel;
}


---

👉 Add in constructor:

add(createHeader(), BorderLayout.NORTH);

⚠️ Then move toolbar into CENTER top section (nested panel)


---

🎛️ Step 4: Modern Toolbar (Purple Accent)

Update button style:

private JButton createAccentButton(String text) {
    JButton btn = new JButton(text);
    btn.setBackground(Theme.ACCENT);
    btn.setForeground(Color.WHITE);
    btn.setFocusPainted(false);
    btn.setFont(new Font("Segoe UI", Font.BOLD, 13));
    btn.setCursor(new Cursor(Cursor.HAND_CURSOR));
    return btn;
}


---

✨ Step 5: Add Section Labels

Add labels like:

JLabel label = new JLabel("Snippets");
label.setForeground(Theme.TEXT_SECONDARY);
label.setFont(new Font("Segoe UI", Font.BOLD, 12));


---

🎯 Step 6: Fix Background Everywhere

Set for ALL panels:

panel.setBackground(Theme.BACKGROUND);


---

🧠 Why Your UI Looked Wrong

Because you had:

No color system ❌

No hierarchy ❌

No spacing rules ❌


Now you have:

Design tokens (Theme) ✅

Accent system ✅

Component states (hover/selected) ✅



---

🔥 What You’ll See After This

Now your UI will:

Feel like a real product

Have purple highlight (like your image)

Look structured and modern

Not like default Swing anymore



---

🚀 Next Level (THIS WILL COMPLETE IT)

If you want it to match EXACTLY like image, next we add:

🎨 SVG Icons (sidebar + buttons)

🧩 Card-based layout

📊 Table redesign (rounded + hover rows)

🌙 Shadow + depth



---

👉 Say: “add icons + polish UI”

And I’ll make it look like something you'd expect from a startup SaaS product UI 😎




Now we’re finally at the stage where it starts looking like a real product UI, not just “styled Swing” 😄

What you’re missing now is:

❌ Icons

❌ Visual depth

❌ Proper spacing hierarchy

❌ Modern table + toolbar feel


Let’s fix that properly (not hacks).


---

🚀 Phase 8: Icons + Final UI Polish

We’ll add:

🎯 What you’ll achieve

🧭 Sidebar with icons (like VS Code)

🎛️ Toolbar with icons

🎨 Purple accent everywhere

📊 Modern table (hover + clean)

🧱 Card-style panels



---

🧩 Step 1: Add Icons (FlatSVG – Best for Swing)

Use:

👉 FlatSVG


---

📥 Download (offline-friendly)

From Maven:

flatlaf-extras-x.x.jar


Add to IntelliJ like H2 jar.


---

🎨 Step 2: Create Icon Loader

📄 IconUtil.java

package com.devworkbench.util;

import com.formdev.flatlaf.extras.FlatSVGIcon;

import javax.swing.*;

public class IconUtil {

    public static Icon get(String name) {
        return new FlatSVGIcon("icons/" + name + ".svg", 16, 16);
    }
}


---

📁 Folder Structure

src/
 └── icons/
      ├── code.svg
      ├── api.svg
      ├── json.svg
      ├── notes.svg
      ├── logs.svg
      ├── settings.svg
      ├── save.svg
      ├── delete.svg
      ├── copy.svg

👉 Download free icons from:

https://tabler-icons.io/

https://heroicons.com/



---

🧭 Step 3: Sidebar with Icons + Selection

Update menu item:

private JButton createMenuItem(String text, String icon) {
    JButton btn = new JButton(text, IconUtil.get(icon));

    btn.setHorizontalAlignment(SwingConstants.LEFT);
    btn.setIconTextGap(10);
    btn.setFocusPainted(false);
    btn.setBorder(BorderFactory.createEmptyBorder(12, 20, 12, 10));
    btn.setFont(new Font("Segoe UI", Font.PLAIN, 14));
    btn.setForeground(Theme.TEXT_SECONDARY);
    btn.setBackground(Theme.SIDEBAR);
    btn.setCursor(new Cursor(Cursor.HAND_CURSOR));

    btn.addActionListener(e -> {
        switchPanel(text);
        highlightSelected(btn);
    });

    btn.addMouseListener(new java.awt.event.MouseAdapter() {
        public void mouseEntered(java.awt.event.MouseEvent evt) {
            if (!btn.getBackground().equals(Theme.ACCENT)) {
                btn.setBackground(Theme.HOVER);
            }
        }

        public void mouseExited(java.awt.event.MouseEvent evt) {
            if (!btn.getBackground().equals(Theme.ACCENT)) {
                btn.setBackground(Theme.SIDEBAR);
            }
        }
    });

    return btn;
}


---

🔥 Use it like:

menuPanel.add(createMenuItem("Snippets", "code"));
menuPanel.add(createMenuItem("API Tester", "api"));
menuPanel.add(createMenuItem("JSON Formatter", "json"));
menuPanel.add(createMenuItem("Notes", "notes"));
menuPanel.add(createMenuItem("Logs", "logs"));
menuPanel.add(createMenuItem("Settings", "settings"));


---

🎛️ Step 4: Toolbar with Icons

Replace buttons:

JButton saveBtn = new JButton("Save", IconUtil.get("save"));
JButton deleteBtn = new JButton("Delete", IconUtil.get("delete"));
JButton copyBtn = new JButton("Copy", IconUtil.get("copy"));


---

✨ Style them:

btn.setBackground(Theme.CARD);
btn.setForeground(Color.WHITE);
btn.setFocusPainted(false);
btn.setBorder(BorderFactory.createEmptyBorder(8, 12, 8, 12));
btn.setCursor(new Cursor(Cursor.HAND_CURSOR));


---

🧱 Step 5: Card UI (This adds depth)

Wrap editor & table inside cards:

private JPanel createCard(JComponent component) {
    JPanel panel = new JPanel(new BorderLayout());
    panel.setBackground(Theme.CARD);
    panel.setBorder(BorderFactory.createEmptyBorder(10, 10, 10, 10));
    panel.add(component);
    return panel;
}


---

👉 Use:

JSplitPane split = new JSplitPane(
    JSplitPane.HORIZONTAL_SPLIT,
    createCard(editorPanel),
    createCard(tableScroll)
);


---

📊 Step 6: Modern Table Styling

table.setRowHeight(32);
table.setShowGrid(false);
table.setIntercellSpacing(new Dimension(0, 0));

table.setBackground(Theme.CARD);
table.setForeground(Color.WHITE);

table.getTableHeader().setBackground(Theme.CARD);
table.getTableHeader().setForeground(Theme.TEXT_SECONDARY);


---

🔥 Row Hover Effect

table.addMouseMotionListener(new java.awt.event.MouseMotionAdapter() {
    public void mouseMoved(java.awt.event.MouseEvent e) {
        int row = table.rowAtPoint(e.getPoint());
        table.setRowSelectionInterval(row, row);
    }
});


---

🎯 Step 7: Final Touches

Add spacing everywhere:

setBorder(BorderFactory.createEmptyBorder(12, 12, 12, 12));


---

Fonts consistency:

UIManager.put("defaultFont", new Font("Segoe UI", Font.PLAIN, 13));


---

🧠 Result After This

Now your app will have:

🔥 Sidebar with icons + hover + selection
🔥 Toolbar with icons
🔥 Purple accent theme
🔥 Card-based layout
🔥 Modern table
🔥 Clean spacing

👉 This is now SaaS-level UI


---

🚀 Final Boss Extensions (Optional but 🔥)

If you want to go even further:

🧠 Auto-complete (like IntelliJ)

🌐 API Tester (Postman clone)

🧾 JSON formatter

🌙 Theme toggle

💾 Export/import snippets



---

👉 Your Move

Run this and tell me:

👉 “Now it looks modern” OR still not satisfied

If still not → I’ll push you to:

🔥 Animation + transitions + collapsible sidebar (ultra pro level)




package com.devworkbench.util;

import java.awt.*;

public class Theme {

    public static final Color BACKGROUND = new Color(24, 26, 31);
    public static final Color SIDEBAR = new Color(20, 22, 26);
    public static final Color CARD = new Color(32, 34, 40);

    public static final Color ACCENT = new Color(130, 90, 255); // 🔥 PURPLE
    public static final Color HOVER = new Color(45, 48, 54);

    public static final Color TEXT_PRIMARY = new Color(220, 220, 220);
    public static final Color TEXT_SECONDARY = new Color(150, 150, 150);

}