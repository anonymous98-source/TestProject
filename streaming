Yes, this one follows the same pattern as the previous two. You can apply the same Reactor-based retry logging approach.

I would make the following changes.

Updated class

package com.cardinalhealth.adv.shipping.integration.api.config;

import com.cardinalhealth.ofl.next.logging.library.LogBuilder;
import lombok.extern.slf4j.Slf4j;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.reactive.function.client.WebClientResponseException;
import reactor.util.retry.Retry;
import reactor.util.retry.Retry.RetrySignal;

import java.time.Duration;

@Slf4j
@Configuration
public class RetryConfig {

    /**
     * Creates a Retry object with a maximum number of attempts and an initial delay.
     *
     * @param maxAttempts the maximum number of retry attempts
     * @param initialDelaySeconds the initial delay in seconds before the first retry
     * @return a Retry object configured with the specified parameters
     */
    public Retry createRetry(int maxAttempts, long initialDelaySeconds) {
        return Retry.backoff(
                    maxAttempts,
                    Duration.ofSeconds(initialDelaySeconds)
                )
                .maxBackoff(Duration.ofSeconds(5))
                .jitter(0.5)
                .filter(this::isRetryable)
                .doBeforeRetry(this::logRetry)
                .onRetryExhaustedThrow((retryBackoffSpec, retrySignal) -> {
                    logRetryExhausted(retrySignal);

                    Throwable lastError = retrySignal.failure();

                    if (lastError instanceof RuntimeException) {
                        return lastError;
                    }

                    return new RuntimeException(
                            "Retries exhausted for the request",
                            lastError
                    );
                });
    }

    /**
     * Determines if a request is retryable based on the exception type and status code.
     * Retry will happen for 5XX errors and also for network issues and unknown exceptions.
     *
     * @param throwable the exception thrown during the request
     * @return true if the request is retryable, false otherwise
     */
    public boolean isRetryable(Throwable throwable) {
        if (throwable instanceof WebClientResponseException ex) {
            int statusCode = ex.getStatusCode().value();

            return statusCode >= 500 && statusCode < 600;
        }

        return true;
    }

    /**
     * Logs exception details for every retry attempt.
     */
    private void logRetry(RetrySignal retrySignal) {

        Throwable failure = retrySignal.failure();

        LogBuilder logBuilder = new LogBuilder()
                .withMessage("Retrying external API request")
                .withField(
                        "retryAttempt",
                        retrySignal.totalRetries() + 1
                )
                .withField(
                        "exceptionType",
                        failure.getClass().getSimpleName()
                )
                .withField(
                        "exceptionMessage",
                        failure.getMessage()
                );

        if (failure instanceof WebClientResponseException ex) {
            logBuilder.withField(
                    "statusCode",
                    ex.getStatusCode().value()
            );
        }

        log.error(logBuilder.toString(), failure);
    }

    /**
     * Logs the final exception when all retry attempts are exhausted.
     */
    private void logRetryExhausted(RetrySignal retrySignal) {

        Throwable failure = retrySignal.failure();

        LogBuilder logBuilder = new LogBuilder()
                .withMessage("Retries exhausted for the request")
                .withField(
                        "totalRetries",
                        retrySignal.totalRetries()
                )
                .withField(
                        "exceptionType",
                        failure.getClass().getSimpleName()
                )
                .withField(
                        "exceptionMessage",
                        failure.getMessage()
                );

        if (failure instanceof WebClientResponseException ex) {
            logBuilder.withField(
                    "statusCode",
                    ex.getStatusCode().value()
            );
        }

        log.error(logBuilder.toString(), failure);
    }
}

What we're changing

The important addition is:

.doBeforeRetry(this::logRetry)

This gives you logging for each actual retry.

And:

logRetryExhausted(retrySignal);

gives you a final log when the retries are exhausted.

We're also removing this from isRetryable():

log.error(new LogBuilder()
        .withMessage("Is this request retryable?")
        .withField("statusCode", statusCode)
        .toString());

because isRetryable() should primarily answer:

Should this exception be retried?

while doBeforeRetry() handles:

A retry is actually happening → log it

Result

For a 503, for example:

API call
   ↓
503
   ↓
isRetryable() → true
   ↓
Retry attempt 1 → LOG
   ↓
503
   ↓
Retry attempt 2 → LOG
   ↓
503
   ↓
Retry attempt 3 → LOG
   ↓
Retries exhausted → LOG final exception

For a 400:

400
 ↓
isRetryable() → false
 ↓
No retry

So this follows the same Reactor-based pattern you've already implemented in P2P Edge, and you don't need to introduce a separate listener class for this application.

Before applying it, I would also do a Find Usages on createRetry() in this application. If multiple WebClient services use this RetryConfig, the change will automatically give all of them the centralized retry logging.