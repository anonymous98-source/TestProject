Great requirement 👍 — this is exactly the right UX polish for a finance app.

We’ll implement this cleanly and visibly, without breaking your logic.


---

🎯 WHAT WE ARE ADDING (CLEAR UX)

In Category section (table):

Visual identification

🔒 Default / Locked categories

Lock icon

Greyed background

Tooltip: “Default category (locked)”


✏ User-added / Unlocked categories

Pencil icon

Normal background

Tooltip: “User editable category”



Result

At a glance, user immediately understands:

What is system-defined

What is custom



---

✅ DESIGN DECISION (IMPORTANT)

We will:

NOT add extra columns (keeps table clean)

Use custom cell renderer

Use icons + background + tooltip

Enforce logic already present (locked flag)



---

1️⃣ NEW ICONS (REQUIRED)

Add these PNGs to your resources:

src/resources/icons/
 ├── lock.png     (16x16)
 └── edit.png     (16x16)

(Any flat icon set works. Keep them small.)


---

2️⃣ CategoryTableModel (NO LOGIC CHANGE)

👉 NO CHANGE REQUIRED
Your existing CategoryTableModel is already correct.

We only enhance UI rendering, not behavior.


---

3️⃣ NEW: CategoryStatusRenderer (CORE UI CHANGE)

CategoryStatusRenderer.java (NEW – FULL CODE)

package com.fundsmanager.ui;

import com.fundsmanager.model.Category;

import javax.swing.*;
import javax.swing.table.DefaultTableCellRenderer;
import java.awt.*;

public class CategoryStatusRenderer extends DefaultTableCellRenderer {

    private final Icon lockIcon;
    private final Icon editIcon;

    public CategoryStatusRenderer() {
        lockIcon = loadIcon("/icons/lock.png");
        editIcon = loadIcon("/icons/edit.png");
        setOpaque(true);
    }

    @Override
    public Component getTableCellRendererComponent(
            JTable table,
            Object value,
            boolean isSelected,
            boolean hasFocus,
            int row,
            int column
    ) {

        super.getTableCellRendererComponent(
                table, value, isSelected, hasFocus, row, column
        );

        CategoryTableModel model =
                (CategoryTableModel) table.getModel();
        Category category = model.getCategories().get(row);

        // -------- ICON & TOOLTIP --------
        if (category.isLocked()) {
            setIcon(lockIcon);
            setToolTipText("Default category (locked)");
        } else {
            setIcon(editIcon);
            setToolTipText("User editable category");
        }

        // -------- BACKGROUND --------
        if (!isSelected) {
            if (category.isLocked()) {
                setBackground(new Color(245, 245, 245)); // subtle grey
            } else {
                setBackground(Color.WHITE);
            }
        }

        // -------- TEXT COLOR --------
        setForeground(Color.DARK_GRAY);

        return this;
    }

    private Icon loadIcon(String path) {
        try {
            return new ImageIcon(
                    CategoryStatusRenderer.class.getResource(path)
            );
        } catch (Exception e) {
            return null;
        }
    }
}


---

4️⃣ MainFrame (UPDATED – SMALL BUT IMPORTANT CHANGE)

We only need to attach the renderer.


---

🔁 UPDATED PART OF MainFrame.java

🔧 Inside createMainContent()

(Replace category table creation)

categoryTable = new JTable(categoryTableModel);
categoryTable.setRowHeight(28);

// 🔥 APPLY STATUS RENDERER
CategoryStatusRenderer statusRenderer =
        new CategoryStatusRenderer();

// Apply renderer to BOTH columns
categoryTable.getColumnModel()
        .getColumn(0)
        .setCellRenderer(statusRenderer);

categoryTable.getColumnModel()
        .getColumn(1)
        .setCellRenderer(statusRenderer);

✅ That’s it.
No other MainFrame changes required.


---

5️⃣ OPTIONAL (VERY NICE UX ADDITION)

Add legend under category table

JLabel legend = new JLabel(
        "🔒 Default Category   |   ✏ User Category"
);
legend.setBorder(BorderFactory.createEmptyBorder(4, 8, 4, 8));
legend.setFont(new Font("Segoe UI", Font.PLAIN, 12));

leftPanel.add(legend, BorderLayout.NORTH);


---

🧠 WHY THIS IS THE RIGHT SOLUTION

✔ No extra columns clutter
✔ No new business logic
✔ Visual cues are immediate
✔ Tooltips help beginners
✔ Scales if more category types are added
✔ Matches banking / enterprise UX


---

🏆 FINAL RESULT (WHAT USER SEES)

| 🔒 Necessary Expenses | 40 | | 🔒 EMIs              | 15 | | ✏ Savings            | 10 | | ✏ Investments        | 10 |

Locked rows look distinct

Editable rows look active

Zero confusion



---

Want next polish?

Disable editing UI for locked rows visually

Inline badge instead of icon

Hover highlight

Context menu (Edit / Delete)


Just say 👍