The error you’re getting:

401 Unauthorized
"ERROR_DESCRIPTION":"Unauthorized : AES decryption Failed!!"

👉 This clearly means: EIS is NOT able to decrypt your payload using the AES key you sent.

So issue is not authentication, it’s encryption mismatch.


---

🔍 Root Cause (Very Important)

Your current implementation:

String aesKey = EisUtil.getRandomSalt(); // UUID → 32 chars
byte[] keybyte = secretKey.getBytes(ENCODING);
SecretKey secretKeyObject = new SecretKeySpec(keybyte, AES_ALGO);

❌ Problem 1: Invalid AES Key Length

AES supports only:

16 bytes (128-bit)

24 bytes (192-bit)

32 bytes (256-bit)


👉 Your UUID = 32 characters = 32 bytes (string)
BUT encoding matters → sometimes becomes invalid key size or unexpected


---

❌ Problem 2: IV is WRONG

byte[] ivKey = Arrays.copyOf(keybyte, 16);

👉 You are:

deriving IV from key ❌

using same IV every time ❌


👉 Most APIs expect:

Random IV (16 bytes)

Sent along with payload



---

❌ Problem 3: EIS may expect different AES mode

You are using:

AES/CBC/PKCS5Padding

But your commented code shows:

AES/GCM/NoPadding

👉 Many secure APIs (especially banking) expect GCM, not CBC.


---

❌ Problem 4: Access Token AES Key mismatch

You:

encryptAccessToken(aesKey, publicKey);

👉 If AES key format is wrong → EIS decrypts wrong key → payload decryption fails.


---

✅ What EIS is EXPECTING (Typical SBI/EIS Pattern)

Most such APIs expect:

✔ AES Key

Exactly 16 bytes


byte[] key = new byte[16];
new SecureRandom().nextBytes(key);


---

✔ IV

Random 16 bytes


byte[] iv = new byte[16];
new SecureRandom().nextBytes(iv);


---

✔ Payload Format

Usually:

Base64( IV + CipherText )


---

✅ FIX (Correct Implementation)

🔧 Step 1: Generate proper AES key

Replace this:

String aesKey = EisUtil.getRandomSalt();

WITH:

byte[] aesKeyBytes = new byte[16]; // 128-bit
new SecureRandom().nextBytes(aesKeyBytes);
String aesKey = Base64.getEncoder().encodeToString(aesKeyBytes);


---

🔧 Step 2: Fix encryption method

public static String encryptPayload(String plainText, byte[] keyBytes) {
    try {
        byte[] iv = new byte[16];
        new SecureRandom().nextBytes(iv);

        Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");
        SecretKeySpec key = new SecretKeySpec(keyBytes, "AES");

        cipher.init(Cipher.ENCRYPT_MODE, key, new IvParameterSpec(iv));

        byte[] encrypted = cipher.doFinal(plainText.getBytes(StandardCharsets.UTF_8));

        // Combine IV + Ciphertext
        byte[] combined = new byte[iv.length + encrypted.length];
        System.arraycopy(iv, 0, combined, 0, iv.length);
        System.arraycopy(encrypted, 0, combined, iv.length, encrypted.length);

        return Base64.getEncoder().encodeToString(combined);

    } catch (Exception e) {
        throw new RuntimeException(e);
    }
}


---

🔧 Step 3: Fix decryption

public static String decryptPayload(String encryptedText, byte[] keyBytes) {
    try {
        byte[] decoded = Base64.getDecoder().decode(encryptedText);

        byte[] iv = Arrays.copyOfRange(decoded, 0, 16);
        byte[] cipherText = Arrays.copyOfRange(decoded, 16, decoded.length);

        Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");
        SecretKeySpec key = new SecretKeySpec(keyBytes, "AES");

        cipher.init(Cipher.DECRYPT_MODE, key, new IvParameterSpec(iv));

        return new String(cipher.doFinal(cipherText), StandardCharsets.UTF_8);

    } catch (Exception e) {
        throw new RuntimeException(e);
    }
}


---

🔧 Step 4: Access Token Fix

String accessToken = encryptAccessToken(
    Base64.getEncoder().encodeToString(aesKeyBytes),
    publicKey
);


---

🚨 MOST COMMON REAL ISSUE (Seen in SBI Projects)

👉 90% cases this error happens because:

❗ IV is not sent OR incorrect format

OR

❗ AES key encoding mismatch (Base64 vs raw)


---

✅ Checklist (Quick Debug)

✔ AES key = 16 bytes exactly
✔ IV = random per request
✔ IV included in payload
✔ Same AES key used for:

payload encryption

RSA encryption (access token)


✔ Encoding:

Always UTF-8

Always Base64 for transfer



---

🔥 If still not working

Then 100% one of these is wrong:

1. EIS expects GCM instead of CBC


2. Payload format expected is:

Base64(ciphertext) + ":" + Base64(iv)


3. AES key must be plain string (not Base64)




---

👉 Tell me this (important)

Send me:

EIS documentation (or screenshot)

Sample request from Postman / working system


I’ll give you exact working code (production ready) for your case.