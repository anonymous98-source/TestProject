If you've added:

boolean existsByCustomerId(UUID customerId);

before calling:

findSubscriptionDetails(customerId, accountNumber)

then you need 3 service test cases now.


---

1. Customer Exists + Subscription Exists

@Test
void shouldReturnSubscriptionDetails() {

    UUID customerId = UUID.randomUUID();
    String accountNumber = "123456";

    FedexSubscriptionProjection projection =
            mock(FedexSubscriptionProjection.class);

    UUID registrationId = UUID.randomUUID();
    UUID subscriptionId = UUID.randomUUID();

    when(projection.getRegistrationId())
            .thenReturn(registrationId);

    when(projection.getSubscriptionId())
            .thenReturn(subscriptionId);

    when(projection.getAccountNumber())
            .thenReturn(accountNumber);

    when(repository.existsByCustomerId(customerId))
            .thenReturn(true);

    when(repository.findSubscriptionDetails(
            customerId,
            accountNumber))
            .thenReturn(Optional.of(projection));

    FedexSubscriptionResponse response =
            service.getFedexSubscription(
                    customerId,
                    accountNumber);

    assertNotNull(response);
    assertEquals(registrationId, response.registrationId());
    assertEquals(subscriptionId, response.subscriptionId());
    assertEquals(accountNumber, response.accountNumber());
}


---

2. Customer Does Not Exist

@Test
void shouldReturnNullWhenCustomerDoesNotExist() {

    UUID customerId = UUID.randomUUID();
    String accountNumber = "123456";

    when(repository.existsByCustomerId(customerId))
            .thenReturn(false);

    FedexSubscriptionResponse response =
            service.getFedexSubscription(
                    customerId,
                    accountNumber);

    assertNull(response);

    verify(repository, never())
            .findSubscriptionDetails(any(), any());
}

Important Verification

verify(repository, never())
        .findSubscriptionDetails(any(), any());

This proves your service exits early and does not execute the second DB query.


---

3. Customer Exists But Subscription Not Found

@Test
void shouldReturnNullWhenSubscriptionNotFound() {

    UUID customerId = UUID.randomUUID();
    String accountNumber = "123456";

    when(repository.existsByCustomerId(customerId))
            .thenReturn(true);

    when(repository.findSubscriptionDetails(
            customerId,
            accountNumber))
            .thenReturn(Optional.empty());

    FedexSubscriptionResponse response =
            service.getFedexSubscription(
                    customerId,
                    accountNumber);

    assertNull(response);
}


---

If you're throwing an exception

If you changed:

return null;

to:

throw new ResourceNotFoundException(...)

then test #2 becomes:

@Test
void shouldThrowExceptionWhenCustomerDoesNotExist() {

    UUID customerId = UUID.randomUUID();

    when(repository.existsByCustomerId(customerId))
            .thenReturn(false);

    assertThrows(
            ResourceNotFoundException.class,
            () -> service.getFedexSubscription(
                    customerId,
                    "123456"));
}


---

For code coverage and review, these 3 scenarios are exactly what I'd expect:

✅ Customer exists + account exists
✅ Customer does not exist
✅ Customer exists + account does not exist

That fully covers the new business logic.