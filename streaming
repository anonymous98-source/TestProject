Yes, if this is just your local/dev database and those 10 rows are test data, the simplest approach is:

Drop child table first

Because of the foreign key:

DROP TABLE IF EXISTS fedex_subscribed_accounts;

Then drop parent table

DROP TABLE IF EXISTS fedex_subscriptions;

Or in one go:

DROP TABLE IF EXISTS fedex_subscribed_accounts;
DROP TABLE IF EXISTS fedex_subscriptions;


---

Then clean Flyway history

Check if Flyway has already executed your migration:

SELECT *
FROM flyway_schema_history
ORDER BY installed_rank DESC;

If your migration is something like:

V20_create_tables_fedex_subscription.sql

then either:

Option 1 (Recommended for local)

Delete the entry:

DELETE FROM flyway_schema_history
WHERE version = '20';

Option 2

Run:

./gradlew flywayClean

(if your project allows clean)


---

Then update your DDL

Change the columns to:

customer_id TEXT
registration_id TEXT
subscription_id TEXT
account_number TEXT

Keep:

id UUID
created_at TIMESTAMP
modified_at TIMESTAMP


---

Finally restart application

./gradlew bootRun

or

./gradlew flywayMigrate

Flyway will recreate the tables from scratch using the updated DDL.

Before doing this

Ask yourself:

Is this your local schema only? → Safe to drop.

Is this a shared DEV environment used by the team? → Don't drop; create a new migration instead.


For a first Jira card, if you're unsure, send a quick Teams message:

> "The tables are already created in the dev schema. Since the data is only test data, is it okay if I drop and recreate them with the updated column types, or would you prefer a Flyway alter migration?"



That avoids surprises.

The error tells you exactly what's wrong:

fedex_subscribed_accounts does not exist, skipping

But the constraint still exists in:

advship_preferences_api.fedex_subscribed_accounts

Looking at your screenshot, you're connected to:

db_local.public

while the actual tables are in:

advship_preferences_api

schema.

First verify

Run:

SELECT schemaname, tablename
FROM pg_tables
WHERE tablename IN (
    'fedex_subscriptions',
    'fedex_subscribed_accounts'
);

You'll likely see:

advship_preferences_api | fedex_subscriptions
advship_preferences_api | fedex_subscribed_accounts

Then drop properly

DROP TABLE IF EXISTS advship_preferences_api.fedex_subscribed_accounts;

DROP TABLE IF EXISTS advship_preferences_api.fedex_subscriptions;

Quickest option

DROP TABLE IF EXISTS advship_preferences_api.fedex_subscriptions CASCADE;

CASCADE will automatically remove:

fk_fedex_subscribed_accounts_subscription

and any dependent objects.

After dropping

Check:

SELECT *
FROM flyway_schema_history
WHERE script LIKE '%fedex%';

If your migration is already marked as executed, delete that entry (only in local):

DELETE
FROM flyway_schema_history
WHERE version = '20';

(or whatever version created these tables).

Then restart:

./gradlew bootRun

or

./gradlew flywayMigrate

and Flyway will recreate the tables using your updated TEXT column definitions.

From the screenshot, the main issue is that you're dropping tables from the wrong schema (public), while the tables live in advship_preferences_api.