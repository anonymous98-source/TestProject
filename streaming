Got it 👍 — that feedback makes total sense.
What you have right now works, but it looks like a 2008 admin tool 😅
Let’s turn this into something that feels modern, clean, and impressive, while still staying in Swing.

Below I’ll give you:

✅ Modern UI approach (FlatLaf – industry-grade look & feel)
✅ Dashboard-style layout
✅ Tabular Report (JTable) + Pie Chart together
✅ Better spacing, typography, cards
✅ Full working updated code (drop-in replacement)


---

🔥 UI Upgrade Strategy (What we’ll change)

1️⃣ Use FlatLaf (Modern Swing Look)

This is what IntelliJ uses internally.
It instantly makes Swing look professional.

Dependency

<dependency>
    <groupId>com.formdev</groupId>
    <artifactId>flatlaf</artifactId>
    <version>3.3</version>
</dependency>

Or download jar and add to classpath.


---

2️⃣ New Layout (Dashboard Style)

┌────────────────────────────────────────────┐
│  Funds Management Dashboard                │
│  Monthly Income: [ 50000 ]   Generate ▶    │
└────────────────────────────────────────────┘

┌───────────────┬────────────────────────────┐
│ Categories    │ Allocation Report           │
│ (Editable)    │ ┌──────────────┐           │
│ JTable        │ │  Pie Chart   │           │
│               │ └──────────────┘           │
│ + Add Cat     │ JTable (Report)             │
└───────────────┴────────────────────────────┘


---

3️⃣ What “Generate Report” does now

✔ Validates
✔ Generates tabular report (Category | % | Amount)
✔ Updates pie chart
✔ Both visible together


---

🧩 New / Updated Components


---

1️⃣ Report Table Model (NEW)

ReportTableModel.java

package com.fundsmanager.ui;

import com.fundsmanager.model.FundAllocation;

import javax.swing.table.AbstractTableModel;
import java.util.List;

public class ReportTableModel extends AbstractTableModel {

    private final String[] columns = {"Category", "Percentage (%)", "Amount"};
    private List<FundAllocation> data;

    public void setData(List<FundAllocation> data) {
        this.data = data;
        fireTableDataChanged();
    }

    public int getRowCount() {
        return data == null ? 0 : data.size();
    }

    public int getColumnCount() {
        return columns.length;
    }

    public String getColumnName(int col) {
        return columns[col];
    }

    public Object getValueAt(int row, int col) {
        FundAllocation fa = data.get(row);
        return switch (col) {
            case 0 -> fa.getCategory();
            case 1 -> fa.getPercentage();
            default -> String.format("₹ %.2f", fa.getAmount());
        };
    }
}


---

2️⃣ Pie Chart Panel (Improved Card Style)

PieChartPanel.java (UPDATED)

package com.fundsmanager.ui;

import com.fundsmanager.model.FundAllocation;
import org.jfree.chart.ChartFactory;
import org.jfree.chart.ChartPanel;
import org.jfree.chart.JFreeChart;
import org.jfree.data.general.DefaultPieDataset;

import javax.swing.*;
import java.awt.*;
import java.util.List;

public class PieChartPanel extends JPanel {

    public PieChartPanel() {
        setLayout(new BorderLayout());
        setBorder(BorderFactory.createTitledBorder("Fund Distribution"));
    }

    public void updateChart(List<FundAllocation> allocations) {
        removeAll();

        DefaultPieDataset dataset = new DefaultPieDataset();
        allocations.forEach(a ->
                dataset.setValue(a.getCategory(), a.getAmount())
        );

        JFreeChart chart = ChartFactory.createPieChart(
                null,
                dataset,
                true,
                true,
                false
        );

        ChartPanel chartPanel = new ChartPanel(chart);
        add(chartPanel, BorderLayout.CENTER);

        revalidate();
        repaint();
    }
}


---

3️⃣ MainFrame (🔥 Major UI Upgrade)

MainFrame.java (REPLACEMENT)

package com.fundsmanager.ui;

import com.fundsmanager.model.Category;
import com.fundsmanager.model.FundAllocation;
import com.fundsmanager.service.FundCalculationService;
import com.fundsmanager.util.DefaultCategories;

import javax.swing.*;
import java.awt.*;
import java.util.List;

public class MainFrame extends JFrame {

    private JTextField incomeField;
    private CategoryTableModel categoryTableModel;
    private ReportTableModel reportTableModel;
    private PieChartPanel pieChartPanel;

    private FundCalculationService service = new FundCalculationService();

    public MainFrame() {
        setTitle("Funds Management Dashboard");
        setSize(1100, 650);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setLayout(new BorderLayout(10, 10));

        add(createHeader(), BorderLayout.NORTH);
        add(createMainContent(), BorderLayout.CENTER);
    }

    private JPanel createHeader() {
        JPanel header = new JPanel(new BorderLayout());
        header.setBorder(BorderFactory.createEmptyBorder(10, 15, 10, 15));

        JLabel title = new JLabel("Funds Management Dashboard");
        title.setFont(new Font("Segoe UI", Font.BOLD, 22));

        JPanel controls = new JPanel(new FlowLayout(FlowLayout.RIGHT));
        incomeField = new JTextField(10);
        JButton generateBtn = new JButton("Generate Report");

        generateBtn.addActionListener(e -> generateReport());

        controls.add(new JLabel("Monthly Income:"));
        controls.add(incomeField);
        controls.add(generateBtn);

        header.add(title, BorderLayout.WEST);
        header.add(controls, BorderLayout.EAST);

        return header;
    }

    private JSplitPane createMainContent() {

        // LEFT: Categories
        categoryTableModel = new CategoryTableModel(DefaultCategories.load());
        JTable categoryTable = new JTable(categoryTableModel);

        JButton addCategoryBtn = new JButton("+ Add Category");
        addCategoryBtn.addActionListener(e -> addCategory());

        JPanel left = new JPanel(new BorderLayout(5, 5));
        left.setBorder(BorderFactory.createTitledBorder("Categories"));
        left.add(new JScrollPane(categoryTable), BorderLayout.CENTER);
        left.add(addCategoryBtn, BorderLayout.SOUTH);

        // RIGHT: Report + Chart
        pieChartPanel = new PieChartPanel();

        reportTableModel = new ReportTableModel();
        JTable reportTable = new JTable(reportTableModel);

        JPanel right = new JPanel(new BorderLayout(5, 5));
        right.setBorder(BorderFactory.createTitledBorder("Allocation Report"));
        right.add(pieChartPanel, BorderLayout.NORTH);
        right.add(new JScrollPane(reportTable), BorderLayout.CENTER);

        JSplitPane splitPane = new JSplitPane(
                JSplitPane.HORIZONTAL_SPLIT,
                left,
                right
        );
        splitPane.setDividerLocation(400);

        return splitPane;
    }

    private void addCategory() {
        AddCategoryDialog dialog = new AddCategoryDialog(this);
        dialog.setVisible(true);

        Category c = dialog.getCategory();
        if (c != null) {
            categoryTableModel.getCategories().add(c);
            categoryTableModel.fireTableDataChanged();
        }
    }

    private void generateReport() {
        double income;
        try {
            income = Double.parseDouble(incomeField.getText());
        } catch (Exception e) {
            JOptionPane.showMessageDialog(this, "Enter valid income");
            return;
        }

        List<Category> categories = categoryTableModel.getCategories();

        if (!service.validatePercentage(categories)) {
            JOptionPane.showMessageDialog(this, "Total percentage must be exactly 100%");
            return;
        }

        List<FundAllocation> allocations =
                service.calculate(income, categories);

        reportTableModel.setData(allocations);
        pieChartPanel.updateChart(allocations);
    }
}


---

4️⃣ Enable FlatLaf (🔥 Instant UI Upgrade)

FundsManagerApp.java (UPDATED)

package com.fundsmanager.app;

import com.fundsmanager.ui.MainFrame;
import com.formdev.flatlaf.FlatLightLaf;

import javax.swing.*;

public class FundsManagerApp {

    public static void main(String[] args) {
        FlatLightLaf.setup();

        SwingUtilities.invokeLater(() ->
                new MainFrame().setVisible(true)
        );
    }
}


---

🧠 What You Have Now (Big Upgrade)

✅ Modern professional UI (FlatLaf)
✅ Dashboard layout (split panes, titled cards)
✅ Editable category table
✅ Tabular report + Pie chart together
✅ Clean spacing, fonts, borders
✅ Interview-ready & demo-ready

This now looks like something a bank / finance tool would ship.


---

Want to go next level? 😎

I can:

Add Dark Mode toggle

Export report to Excel / PDF

Persist data using SQLite

Convert this to JavaFX Material UI

Package as Windows EXE


Just say the word 🔥