Perfect—here’s your fully upgraded SnippetPanel class with:

Dark-theme friendly UI (works beautifully with FlatLaf)

Clean layout (split pane, spacing)

Styled editor (IDE-like feel)

Search + CRUD (already wired)

Better UX (selection, tooltips, buttons)


👉 Replace your existing SnippetPanel.java completely with this:


---

package com.devworkbench.ui.snippet;

import com.devworkbench.model.Snippet;
import com.devworkbench.service.SnippetService;

import javax.swing.*;
import javax.swing.event.ListSelectionEvent;
import javax.swing.table.DefaultTableModel;
import java.awt.*;
import java.util.List;

public class SnippetPanel extends JPanel {

    private JTextField titleField;
    private JTextArea codeArea;
    private JTextField searchField;
    private JTable table;
    private DefaultTableModel tableModel;

    private final SnippetService service = new SnippetService();
    private int selectedId = -1;

    public SnippetPanel() {
        setLayout(new BorderLayout(10, 10));
        setBorder(BorderFactory.createEmptyBorder(10, 10, 10, 10));

        // 🔍 Top Search Panel
        JPanel topPanel = new JPanel(new BorderLayout(8, 8));
        searchField = new JTextField();
        searchField.setToolTipText("Search snippets...");
        JButton searchBtn = new JButton("Search");

        topPanel.add(searchField, BorderLayout.CENTER);
        topPanel.add(searchBtn, BorderLayout.EAST);

        add(topPanel, BorderLayout.NORTH);

        // 🧾 Form Panel
        JPanel formPanel = new JPanel();
        formPanel.setLayout(new BorderLayout(10, 10));

        titleField = new JTextField();
        titleField.setToolTipText("Enter snippet title...");

        codeArea = new JTextArea();
        codeArea.setFont(new Font("Consolas", Font.PLAIN, 14));
        codeArea.setLineWrap(true);
        codeArea.setWrapStyleWord(true);

        // IDE-like dark editor feel
        codeArea.setBackground(new Color(30, 30, 30));
        codeArea.setForeground(new Color(220, 220, 220));
        codeArea.setCaretColor(Color.WHITE);

        JPanel titlePanel = new JPanel(new BorderLayout(5, 5));
        titlePanel.add(new JLabel("Title"), BorderLayout.NORTH);
        titlePanel.add(titleField, BorderLayout.CENTER);

        JPanel codePanel = new JPanel(new BorderLayout(5, 5));
        codePanel.add(new JLabel("Code"), BorderLayout.NORTH);
        codePanel.add(new JScrollPane(codeArea), BorderLayout.CENTER);

        formPanel.add(titlePanel, BorderLayout.NORTH);
        formPanel.add(codePanel, BorderLayout.CENTER);

        // 📊 Table
        tableModel = new DefaultTableModel(new String[]{"ID", "Title", "Code"}, 0);
        table = new JTable(tableModel);

        table.setRowHeight(28);
        table.setFont(new Font("Segoe UI", Font.PLAIN, 13));
        table.getTableHeader().setFont(new Font("Segoe UI", Font.BOLD, 14));
        table.setSelectionMode(ListSelectionModel.SINGLE_SELECTION);

        JScrollPane tableScroll = new JScrollPane(table);

        // 🧩 Split Pane
        JSplitPane splitPane = new JSplitPane(JSplitPane.HORIZONTAL_SPLIT, formPanel, tableScroll);
        splitPane.setDividerLocation(350);
        splitPane.setResizeWeight(0.4);

        add(splitPane, BorderLayout.CENTER);

        // 🎛️ Buttons
        JPanel buttonPanel = new JPanel(new FlowLayout(FlowLayout.RIGHT, 10, 5));

        JButton saveBtn = new JButton("Save");
        JButton updateBtn = new JButton("Update");
        JButton deleteBtn = new JButton("Delete");
        JButton clearBtn = new JButton("Clear");

        // Remove ugly focus border
        saveBtn.setFocusPainted(false);
        updateBtn.setFocusPainted(false);
        deleteBtn.setFocusPainted(false);
        clearBtn.setFocusPainted(false);

        buttonPanel.add(saveBtn);
        buttonPanel.add(updateBtn);
        buttonPanel.add(deleteBtn);
        buttonPanel.add(clearBtn);

        add(buttonPanel, BorderLayout.SOUTH);

        // 🔗 Actions
        saveBtn.addActionListener(e -> saveSnippet());
        updateBtn.addActionListener(e -> updateSnippet());
        deleteBtn.addActionListener(e -> deleteSnippet());
        clearBtn.addActionListener(e -> clearForm());
        searchBtn.addActionListener(e -> search());

        table.getSelectionModel().addListSelectionListener(this::onRowSelect);

        loadData();
    }

    private void saveSnippet() {
        service.addSnippet(titleField.getText(), codeArea.getText());
        clearForm();
        loadData();
    }

    private void updateSnippet() {
        if (selectedId != -1) {
            service.updateSnippet(selectedId, titleField.getText(), codeArea.getText());
            clearForm();
            loadData();
        }
    }

    private void deleteSnippet() {
        if (selectedId != -1) {
            int confirm = JOptionPane.showConfirmDialog(this,
                    "Delete this snippet?", "Confirm",
                    JOptionPane.YES_NO_OPTION);

            if (confirm == JOptionPane.YES_OPTION) {
                service.deleteSnippet(selectedId);
                clearForm();
                loadData();
            }
        }
    }

    private void search() {
        String keyword = searchField.getText();
        List<Snippet> list = service.searchSnippets(keyword);
        refreshTable(list);
    }

    private void loadData() {
        refreshTable(service.getAllSnippets());
    }

    private void refreshTable(List<Snippet> list) {
        tableModel.setRowCount(0);

        for (Snippet s : list) {
            tableModel.addRow(new Object[]{
                    s.getId(),
                    s.getTitle(),
                    s.getCode()
            });
        }
    }

    private void clearForm() {
        titleField.setText("");
        codeArea.setText("");
        selectedId = -1;
        table.clearSelection();
    }

    private void onRowSelect(ListSelectionEvent e) {
        int row = table.getSelectedRow();
        if (row >= 0) {
            selectedId = (int) tableModel.getValueAt(row, 0);
            titleField.setText((String) tableModel.getValueAt(row, 1));
            codeArea.setText((String) tableModel.getValueAt(row, 2));
        }
    }
}


---

🎯 What You’ll Notice Now

🔥 Dark editor feel (like IDE)

🧩 Clean split layout

📊 Better table readability

🎛️ Professional button layout

⚡ Smooth UX (select → edit → update)



---

🚀 Next Upgrade (THIS is where it becomes insane)

If you want real “developer tool” feel, next we add:

👉 Syntax highlighting using:

RSyntaxTextArea
👉 Sidebar navigation
👉 Keyboard shortcuts



---

Just say: “next level UI”
and I’ll take this from good → elite 🔥